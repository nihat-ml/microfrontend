services:
  remote-app:
    build: ./remote-app
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
  host-app:
    build: ./host-app
    ports:
      - "5174:5174"
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - remote-app